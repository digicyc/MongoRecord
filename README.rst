============
MongoRecord
============

MongoRecord is just a basic Lift package for starting a Lift project completely based around MongoDB as
the backend DB. This also includes ProtoUser connected to a MongoDB.
